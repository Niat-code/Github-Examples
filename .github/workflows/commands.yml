name: "Commands"

on: 
    push:
        branches:
            - main

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: "group logging"
              run: |
                echo "Hello"
                echo "World"